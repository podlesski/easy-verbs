import Foundation

class UniqueButton {
    let name: String
    let side: Int
    let id: String
    
    init(name: String, side: Int, id: String) {
        self.name = name
        self.side = side
        self.id = id
    }
}
